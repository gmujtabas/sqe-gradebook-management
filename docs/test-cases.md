# GradeBook Test Cases

| ID     | Title                                     | Requirement | Preconditions                                                                                 | Steps                                                                                                                          | Expected                                                                                                       | Priority | Type                  | Status           | Execution Note                                                                            | GitHub Issue     |
| ------ | ----------------------------------------- | ----------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- | -------- | --------------------- | ---------------- | ----------------------------------------------------------------------------------------- | ---------------- |
| TC-001 | Add valid score                           | REQ-3       | A valid Student object exists with fewer than 6 scores.                                       | 1. Call `student.add_score(75)`.<br>2. Check the student's scores list.                                                        | Score `75` is added successfully to the scores list. No error is raised.                                       | High     | Positive / Functional | **Pass**         | Score 75 was successfully added and appeared in the scores list.                          | —                |
| TC-002 | Reject negative score                     | REQ-3       | A valid Student object exists with an empty scores list.                                      | 1. Call `student.add_score(-5)`.<br>2. Check the scores list.                                                                  | A `ValueError` is raised and the scores list remains empty.                                                    | High     | Negative / Functional | **Pass**         | Adding -5 raised `IllegalArgumentException`, so the negative score was rejected.          | —                |
| TC-003 | Reject non-numeric score                  | REQ-3       | A valid Student object exists with an empty scores list.                                      | 1. Call `student.add_score("abc")`.<br>2. Check the scores list.                                                               | The invalid non-numeric score is rejected with an appropriate error, and the scores list remains unchanged.    | High     | Negative / Functional | **Blocked**      | `addScore()` accepts only an integer parameter, so a non-numeric string cannot be tested. | —                |
| TC-004 | Calculate average with multiple scores    | REQ-6       | A Student object has scores `60`, `70`, and `80`.                                             | 1. Call `student.average()`.<br>2. Record the returned value.                                                                  | The method returns the arithmetic mean of the scores, which is `70.0`.                                         | High     | Positive / Functional | **Pass**         | Scores 60, 70, and 80 produced an average of 70.0.                                        | —                |
| TC-005 | Average with empty score list             | REQ-6       | A valid Student object has no recorded scores.                                                | 1. Call `student.average()`.<br>2. Record the returned value.                                                                  | The method returns `0.0` without raising an error.                                                             | High     | Boundary / Functional | **Pass**         | A student with no scores returned an average of 0.0.                                      | —                |
| TC-006 | Average with a single score               | REQ-6       | A valid Student object has exactly one score: `85`.                                           | 1. Call `student.average()`.<br>2. Record the returned value.                                                                  | The method returns `85.0`.                                                                                     | Medium   | Positive / Functional | **Pass**         | A single score of 85 produced an average of 85.0.                                         | —                |
| TC-007 | Reject duplicate roll number              | REQ-1       | A GradeBook already contains a student with roll number `101`.                                | 1. Attempt to add another student with roll number `101`.<br>2. Observe the result.                                            | The second student is rejected and the original student remains on the roster.                                 | High     | Negative / Functional | **Pass**         | The GradeBook checks for an existing student ID and rejects duplicate IDs.                | —                |
| TC-008 | Handle student name case-insensitively    | REQ-2       | A GradeBook contains a valid student name such as `Ali Khan`.                                 | 1. Add or search for the student using a different letter case, such as `ali khan`.<br>2. Observe the result.                  | The system treats the name without regard to letter case and correctly handles the equivalent name.            | Medium   | Functional            | **Fail**         | The current implementation does not provide case-insensitive name handling.               | #21 |
| TC-009 | Accept maximum score boundary             | REQ-4       | A valid Student object exists with fewer than 6 scores.                                       | 1. Call `student.add_score(100)`.<br>2. Check the scores list.                                                                 | Score `100` is accepted and added successfully because `100` is the maximum valid score.                       | High     | Boundary / Functional | **Pass**         | Score 100 was accepted successfully.                                                      | —                |
| TC-010 | Accept minimum score boundary             | REQ-4       | A valid Student object exists with fewer than 6 scores.                                       | 1. Call `student.add_score(0)`.<br>2. Check the scores list.                                                                   | Score `0` is accepted and added successfully because `0` is the minimum valid score.                           | High     | Boundary / Functional | **Pass**         | Score 0 was accepted successfully.                                                        | —                |
| TC-011 | Convert mid-range average to letter grade | REQ-7       | A Student has an average in the middle of a defined grade range, such as `75.0`.              | 1. Create a student with scores producing an average of `75.0`.<br>2. Call the grade conversion function/method.               | The returned letter grade matches the defined grading schedule for an average of `75.0`.                       | High     | Positive / Functional | **Blocked**      | The current Student class has no letter-grade conversion functionality to test.           | —                |
| TC-012 | Convert grade at boundary                 | REQ-7       | A Student has an average exactly equal to a grade boundary defined in the grading schedule.   | 1. Create scores producing the exact boundary average.<br>2. Call the grade conversion function/method.                        | The returned letter grade matches the grade assigned to that exact boundary according to the grading schedule. | High     | Boundary / Functional | **Blocked**      | The current Student class has no letter-grade conversion functionality to test.           | —                |
| TC-013 | Reject seventh score                      | REQ-5       | A valid Student object already has 6 recorded scores.                                         | 1. Call `student.add_score(75)` to attempt to add a seventh score.<br>2. Check the scores list.                                | The 7th score is rejected with an appropriate error, and the student still has only 6 scores.                  | High     | Negative / Functional | **Not Executed** | Added during Task 3 to close the REQ-5 traceability gap.                                  | —                |
| TC-014 | Calculate class average                   | REQ-8       | A GradeBook contains two or more students with recorded scores and known individual averages. | 1. Calculate each student's individual average.<br>2. Call the class-average function/method.<br>3. Record the returned value. | The class average equals the arithmetic mean of all students' individual averages.                             | High     | Positive / Functional | **Not Executed** | Added during Task 3 to close the REQ-8 traceability gap.                                  | —                |
